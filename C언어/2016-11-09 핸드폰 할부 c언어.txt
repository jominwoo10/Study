#include <stdio.h>

int installment(int a, int b);

int main(void)  
{
	int num1,num2,num3;
	char arr1[]="┌───기종──────가격──┐\n";
	char arr2[]="│  (1) 겔럭시 노트7│ 700,000  │\n";
	char arr3[]="│  (2) 겔럭시 S7   │ 800,000  │\n";
	char arr4[]="│  (3) 아이폰 7    │ 890,000  │\n";
	char arr5[]="│  (4) 아이폰 7+   │ 950,000  │\n";
	char arr6[]="│  (5) 종  료      │          │\n";
	char arr7[]="└───────────────┘\n";

	printf("%s%s%s%s%s%s%s",arr1,arr2,arr3,arr4,arr5,arr6,arr7);
	printf("구매하실 핸드폰 기종을 번호로 입력 : ");
	scanf("%d",&num1);

	if(num1==1)
	{
		printf("(1)12개월 (2)24개월  (3)36개월 \n");
		printf("할부개월수를 입력해주세요 : ");
		scanf("%d",&num2);
			if(num2 == 1)
				printf("핸드폰을 사리려면 한달에 %d원을 내셔야합니다.\n",installment(num2,700000));
			else if(num2 == 2)
				printf("핸드폰을 사리려면 한달에 %d원을 내셔야합니다.\n",installment(num2,700000));
			else if(num2 == 3)
				printf("핸드폰을 사리려면 한달에 %d원을 내셔야합니다.\n",installment(num2,700000));
			else
			{
				printf("할부개월을 제대로 입력하지 않으셧습니다.\n");
				return 0;
			}
	}
	else if(num1==2)
	{
		printf("(1)12개월 (2)24개월  (3)36개월 \n");
		printf("할부개월수를 입력해주세요 : ");
		scanf("%d",&num2);
		if(num2 == 1)
				printf("핸드폰을 사리려면 한달에 %d원을 내셔야합니다.\n",installment(num2,800000));
			else if(num2 == 2)
				printf("핸드폰을 사리려면 한달에 %d원을 내셔야합니다.\n",installment(num2,800000));
			else if(num2 == 3)
				printf("핸드폰을 사리려면 한달에 %d원을 내셔야합니다.\n",installment(num2,800000));
			else
			{
				printf("할부개월을 제대로 입력하지 않으셧습니다.\n");
				return 0;
			}
	}
	else if(num1==3)
	{
		printf("(1)12개월 (2)24개월  (3)36개월 \n");
		printf("할부개월수를 입력해주세요 : ");
		scanf("%d",&num2);
		if(num2 == 1)
				printf("핸드폰을 사리려면 한달에 %d원을 내셔야합니다.\n",installment(num2,890000));
			else if(num2 == 2)
				printf("핸드폰을 사리려면 한달에 %d원을 내셔야합니다.\n",installment(num2,890000));
			else if(num2 == 3)
				printf("핸드폰을 사리려면 한달에 %d원을 내셔야합니다.\n",installment(num2,890000));
			else
			{
				printf("할부개월을 제대로 입력하지 않으셧습니다.\n");
				return 0;
			}
	}
	else if(num1==4)
	{
		printf("(1)12개월 (2)24개월  (3)36개월 \n");
		printf("할부개월수를 입력해주세요 : ");
		scanf("%d",&num2);
		if(num2 == 1)
				printf("핸드폰을 사리려면 한달에 %d원을 내셔야합니다.\n",installment(num2,950000));
			else if(num2 == 2)
				printf("핸드폰을 사리려면 한달에 %d원을 내셔야합니다.\n",installment(num2,950000));
			else if(num2 == 3)
				printf("핸드폰을 사리려면 한달에 %d원을 내셔야합니다.\n",installment(num2,950000));
			else
			{
				printf("할부개월을 제대로 입력하지 않으셧습니다.\n");
				return 0;
			}
	}
	else
	{
		printf("종료됩니다. \n");
		return 0;
	}
	return 0;  
}

int installment(int a, int b)
{
	
	a *= 12;
	if(a==0)
		return 0;
	else
	{
		installment(a,b);
		return b/a;
	}
}